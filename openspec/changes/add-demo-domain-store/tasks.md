## 1. Предметные модели и моки

- [x] 1.1 Добавить восемь TypeScript-моделей и тип ShipmentData в src/domain/shipment/types.ts по описанию предметной области; без ограничений вместимости ТМ и без логики операций.
- [x] 1.2 Добавить createDemoData() в demo-data.ts с согласованными товарами, контейнерами, строками и КМ/КА; каждый вызов возвращает независимые объекты и вложенные массивы, ТМ и распределение пусты.

## 2. Минимальный MobX-стор

- [x] 2.1 Проверить API установленного mobx и реализовать ShipmentStore с собственными observable-данными из переданного набора либо свежих моков; не менять глобальную конфигурацию MobX и не подключать стор к React.
- [x] 2.2 Реализовать computed-итоги заказа, строки с остатками, итоги остатков, распределённое количество и прогресс; учитывать уникальные SKU, положительные остатки контейнеров и нулевое исходное количество.

## 3. Проверка

- [x] 3.1 Сверить модели и моковые данные с согласованным описанием; не добавлять автоматические тесты, тестовый runner или тестовые зависимости.
- [x] 3.2 Запустить pnpm build и pnpm lint; убедиться, что UI не изменён, сеть и browser storage не используются, обработка КМ/КА не добавлена.
