## 1. Выделение границ функциональности

- [x] 1.1 Создать директории для заголовка отгрузки, сводки заказа, статуса распределения, рабочей области распределения и сводки остатка.
- [x] 1.2 Переместить приватные компоненты, константы, хелперы и стили каждого блока в принадлежащую ему директорию.
- [x] 1.3 Оставить `ShipmentPage` корнем композиции и владельцем создания shipment-store.

## 2. Определение локальных публичных API

- [x] 2.1 Добавить `index.ts` в каждый независимо используемый блок страницы отгрузки.
- [x] 2.2 Экспортировать из локальной точки входа только публичный компонент блока и необходимые публичные типы.
- [x] 2.3 Обновить межблочные импорты: использовать конкретные точки входа блоков и удалить импорты приватных файлов.

## 3. Проверка рефакторинга

- [x] 3.1 Убедиться, что нет shipment-wide barrel-файла, реэкспортирующего все блоки страницы.
- [x] 3.2 Запустить релевантные typecheck, lint и UI-тесты или сборку, чтобы подтвердить неизменность поведения.
