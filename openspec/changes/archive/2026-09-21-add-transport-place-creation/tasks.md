## 1. Состояние и команды ShipmentStore

- [x] 1.1 Добавить наблюдаемый `activeTransportPlaceId`, команду создания одного ТМ и команду выбора существующего ТМ; генерировать уникальные локальные ID, `max(sequence) + 1` и номера `ТМ-NNN` без изменения `ShipmentData` и allocationLines.
- [x] 1.2 Подготовить вычисляемое представление строк ТМ в порядке sequence с нулевыми показателями для пустых мест; сохранить расчёты вне JSX и не менять итоги, остатки и прогресс.

## 2. Правая панель транспортных мест

- [x] 2.1 Передать `ShipmentStore` в `DestinationPanel`, подключить `observer` и заменить disabled Dialog-заготовку прямой рабочей кнопкой «Создать ТМ».
- [x] 2.2 Отображать пустое состояние только без ТМ, а после создания — строки с локальными номерами и нулевыми значениями; реализовать доступный выбор и визуальное выделение активного ТМ.
- [x] 2.3 Сохранить `PAL`, `MIX`, переключатели и все операции переноса/возврата disabled; не добавлять удаление, распределение, автоматическое создание при переносе, SSCC или сохранение.

## 3. Проверка

- [x] 3.1 В браузере проверить начальное пустое состояние, последовательное создание `ТМ-001` и `ТМ-002`, автоматическую активацию нового места, выбор первой строки и неизменность исходных итогов/прогресса; после перезагрузки должно восстанавливаться пустое демо.
- [x] 3.2 Проверить компоновку при 1280×720 и 1440×900, клавиатурную доступность выбора, disabled-состояние отложенных действий; выполнить `pnpm lint`, `pnpm format:check` и `pnpm build` с Node 24.21.0 без добавления тестов.

Проверка в браузере: начальное пустое состояние восстанавливается после reload; `ТМ-001` и `ТМ-002` создаются последовательно, новое ТМ активно, выбор первой и второй строки работает мышью, Enter и Space. При 1280×720 и 1440×900 горизонтального переполнения нет; единственная доступная кнопка — «Создать ТМ», остальные 17 кнопок disabled. Остаток остаётся 44 шт., прогресс — 0%, ошибок в консоли нет. `pnpm lint`, `pnpm format:check` и `pnpm build` выполнены с Node 24.21.0.
