## 1. Scanner input presentation

- [x] 1.1 Start `BarcodeInput` in its hidden, focusable presentation and make
  `F2` reveal the scanner input without stealing focus from another text field.
- [x] 1.2 Give `Esc` priority for collapsing the open, focused scanner input
  before workflow cancellation is considered.

## 2. Workspace wording and verification

- [x] 2.1 Rename the existing `return-product` action label to `Вернуть
  строку` without changing its command.
- [x] 2.2 Run the relevant automated checks and strict OpenSpec validation.
