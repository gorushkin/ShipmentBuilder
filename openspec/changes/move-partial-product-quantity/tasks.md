## 1. Частичные команды стора

- [ ] 1.1 Добавить вычисления доступного целого количества для выбранной товарной строки слева и выбранного товара активного ТМ справа.
- [ ] 1.2 Реализовать команду частичного переноса: без фильтра распределять количество по исходным строкам в стабильном порядке, с фильтром — только из выбранной строки.
- [ ] 1.3 Реализовать команду частичного возврата: уменьшать `AllocationLine` выбранного товара в стабильном порядке и удалять нулевые строки.
- [ ] 1.4 Валидировать количество в сторе, сохранять `sourceLineId`, не изменять КМ/КА и после успеха сбрасывать только соответствующий выбор.

## 2. Диалог количества и доступность действий

- [ ] 2.1 Создать переиспользуемый `Dialog` shadcn для частичного переноса и возврата с названием товара, максимумом и полем целого количества.
- [ ] 2.2 Реализовать UI-валидацию значений вне диапазона 1..доступный остаток и блокировку подтверждения до валидного ввода.
- [ ] 2.3 Вывести заглушку о будущем подтверждении КМ/КА для маркируемого товара без блокировки операции.
- [ ] 2.4 Сделать кнопку «Переместить количество» доступной только в контексте выбранной товарной строки и открыть диалог с верным направлением.
- [ ] 2.5 После подтверждения закрывать диалог и сохранять активное ТМ, режимы и фильтр источника.

## 3. Проверка

- [ ] 3.1 Проверить частичный перенос агрегированного товара и одной фильтрованной исходной строки, включая сохранение остатков других контейнеров.
- [ ] 3.2 Проверить частичный возврат товара из нескольких исходных строк, сохранение происхождения и удаление нулевых распределений.
- [ ] 3.3 Проверить доступность кнопки, невалидный ввод, сброс выбора и сохранение рабочего контекста в браузере.
- [ ] 3.4 Запустить `npm run lint`, `npm run build` и `openspec validate move-partial-product-quantity --strict`.
